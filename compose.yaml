name: life-and-money-api

services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=lifeandmoney'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=lifeandmoney'
    ports:
      - '5432'